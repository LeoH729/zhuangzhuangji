# 小程序性能优化测试清单

候选包状态：未上传、未提审  
优先级说明：P0必须全部通过；P1应在提交审核前完成。

## 1. 当前自动验证结果

- [x] 36项自动化契约测试通过。
- [x] 夯/拉与弹窗素材专项回归6项通过，控制台错误0、运行时异常0。
- [x] 微信开发者工具真实运行36项自动化检查通过，控制台错误0、运行时异常0。
- [x] 微信开发者工具已完成本地编译和页面运行验证。
- [x] 33个小程序/相关云函数JS文件语法检查通过。
- [x] 配置JSON解析通过。
- [x] 云端旧版首页接口兼容、数量、排序和详情字段通过。
- [x] 缓存预热、命中、失效代码路径及异常降级通过静态与生产验证。
- [x] 主包源码边界估算下降39.7%。
- [x] 微信开发者工具本地完整编译（CLI服务端口已开启）。
- [ ] 安卓与iPhone真机性能数据。

## 2. P0 启动与首页

- [ ] 冷启动进入老板专区，首屏模板可见且可点击，期间无白屏或长时间空态。
- [ ] 冷启动网络面板中首页业务请求只有一个 `featureConfig/getHome`。
- [ ] 热启动及后台恢复5分钟内不重复请求当前分类。
- [ ] 断网后有缓存时先展示缓存；无缓存时展示明确失败提示，不闪现“0条”。
- [ ] 首批只渲染8张卡片，其余随后补齐，最终数量与线上一致。
- [ ] 老板专区、玩图专区的所有分类可切换，返回顺序与后台“专区 × 分类”一致。
- [ ] NEW/HOT仅展示标签，不改变排序。
- [x] 首页模板卡片使用 👍/👎 显示真实评价数量，零值不隐藏；静态契约通过。
- [ ] 同一模板从不同专区、不同分类进入时，详情、生成和保存埋点记录实际入口的专区与分类。
- [ ] 下拉刷新发起强制刷新，刷新后仍保持当前分类。
- [ ] 卡片曝光仅在真实进入视口时上报，同一卡片/分类/会话不重复上报。
- [ ] 玩图首页按进入场景展示对应诊断Banner：场景1069使用外部广告位，其他场景使用内部广告位。
- [x] 玩图首页诊断Banner位于分类分页栏之后、模板瀑布流之前。
- [ ] 老板专区不展示诊断Banner；首页不加载插屏模块。

## 3. P0 模板与生图

- [ ] 模板详情能从首页、旧分享链接、小程序码正常打开。
- [x] 模板详情使用 👍/👎 显示真实评价数量，并与首页同一模板一致；静态契约通过。
- [ ] 图片上传模板、纯文本模板的必填校验和提交均正常。
- [ ] 免费模板和扣星光模板均可提交，余额不足正确引导星光页。
- [ ] 主能力策略成功、主策略失败转兜底均正常。
- [ ] 生成失败时星光自动退回且只退一次。
- [ ] 验证3:4、4:3、9:16、16:9及提示词指定比例，不得全部变为1:1。
- [ ] 提交后进入等待页；等待页才加载插屏模块，广告失败不能阻止生成。
- [ ] 等待页插屏关闭后页面计时、轮询和小游戏继续正常。
- [ ] 点击生成加速后才创建激励视频；完整观看、提前关闭、无广告、网络失败分别正确处理。
- [ ] 生成完成能进入结果页；结果页分包已在等待期间预下载。

## 4. P0 保存、分享和结果

- [ ] 原图保存点击、成功、下载失败、相册拒绝分别正确记录。
- [ ] 高清保存点击、高清生成失败、下载失败、相册拒绝、保存成功分别正确记录。
- [ ] 打开保存面板不被误记为“保存原图”。
- [ ] 结果预览、👍/👎评价、重复评价拦截正常。
- [ ] 分享好友、朋友圈、海报生成、分享奖励均正常。
- [ ] 旧结果链接和分享结果页能正常打开并“生成同款”。

## 5. P0 星光与支付

- [ ] 新用户首页可看到新人礼包，但进入首页3秒前不查询礼包、不创建激励广告。
- [x] 新人礼包背景已迁移至 CloudBase 版本化资源，并实现下载、持久缓存、预解码和 CSS 兜底；待真机新预览包确认。
- [x] 生成加速弹窗背景已迁移至 CloudBase 版本化资源，并实现详情页提前加载；待真机新预览包确认。
- [ ] 首次安装验证两张背景与弹窗同时出现，不发生空白后补图或布局跳变。
- [ ] 第二次打开验证直接命中本地持久缓存。
- [ ] 弱网/断网验证 CSS 兜底完整可操作，且当前可见弹窗不在网络恢复后突然替换背景。
- [ ] 安卓和 iPhone 真机确认两张 PNG 背景均完整显示、透明区域和按钮位置正确。

## 5.1 P0 图片保存状态后台联动

- [ ] `generated_results/` 图片原图保存成功后，后台“是否被保存”显示“是”。
- [ ] 高清保存成功后，对应 `generated_results/` 原生成图显示“是”。
- [ ] 已接入精确埋点但从未成功保存的图片显示“否”。
- [ ] 权限拒绝、下载失败或相册保存失败不能显示“是”。
- [ ] 历史未接入精确埋点的图片显示“—”，不误判为“否”。
- [ ] `uploads/`、`generated_upscaled/`、调试和历史目录该字段留空。
- [ ] 点击“立即领取”后才加载广告；完整观看只领取一次。
- [ ] 星光余额、流水、分享任务和新人任务显示正常。
- [ ] 六档充值商品、价格、星光数量正确。
- [ ] 支付取消不记成功；支付成功、主动查单、异步到账和补单均只发货一次。
- [ ] 进入星光页前，主包Source Map不包含虚拟支付实现。

## 6. P0 任务恢复与生命周期

- [ ] 有1、5、20个遗留任务时只调用一次 `getTaskStatuses`，不逐条串行请求。
- [ ] 无遗留任务时不启动轮询定时器。
- [ ] 超过24小时的本地遗留任务自动清理；最多保留20个最新任务。
- [ ] 全部任务完成后停止轮询；小程序进入后台后停止全局轮询，恢复前台后按需继续。
- [ ] 完成通知横幅在首页、个人中心正确显示；等待页、结果页和历史页不重复显示。

## 7. P1 图片缓存与弱网

- [ ] 首屏构建时无同步文件扫描、同步删除或同步元数据写入。
- [ ] 首次网络图加载成功后异步缓存；第二次启动可命中缓存。
- [ ] 缓存文件损坏时只删除该项并回退网络图，不阻塞其他卡片。
- [ ] 单张卡片加载完成只更新对应字段，开发者工具中无整列大对象 `setData`。
- [ ] 元数据写入频率不高于每2秒一次；切后台时补写。
- [ ] `home_card`、`detail_banner`、`task_thumb`、`result_preview`均返回对应压缩图。
- [ ] 2G/弱网、网络切换、图片404和云调用超时下页面可恢复。

## 8. P1 其他业务回归

- [ ] 个人中心头像、注册时间、星光、任务列表正常。
- [ ] 反馈页只提交类型和内容，请求中不包含邮箱、手机号或其他联系方式。
- [ ] 小程序反馈列表和运营后台均不显示历史联系方式，反馈状态处理正常。
- [ ] 管理员鉴权只在反馈管理入口需要时发生，不出现在冷启动关键路径。
- [ ] 老板专区、玩图专区、个人中心Tab切换正常。
- [ ] 页面分包首次进入有合理加载反馈，二次进入无异常。
- [ ] 页面URL保持不变，历史分享路径全部兼容。

## 9. 性能采样方法

每种机型清缓存后冷启动20次，记录：代码包加载、JS注入、首屏内容可见、首屏可交互、`getHome`耗时、图片首张成功时间、异常退出。机型至少覆盖：

- [ ] 安卓低端机1台。
- [ ] 安卓主流机1台。
- [ ] iPhone 1台。

通过门槛：本地冷启动均值较线上V1.4.7下降至少35%；提交审核前所有P0通过且无阻断级回归。
