# 项目目录隔离迁移记录

迁移日期：2026-08-21

## 迁移结果

- 小程序源码由项目根目录迁移至 `miniprogram/`。
- `project.config.json` 已固定 `miniprogramRoot=miniprogram/`。
- 开发与私有配置均开启 `ignoreDevUnusedFiles`。
- 运营后台保留在 `admin-web/`，位于小程序编译根之外。
- 云函数保留在 `cloudfunctions/`，位于小程序编译根之外。
- 网站从原 `web-app/` 移至同级独立项目 `F:\OPC_SuperSolo\Ai造梦馆\zhuangzhuangji-web`。
- 网站增加独立 `.gitignore`，自行管理 `.next`、`node_modules`、环境变量、日志和构建缓存。
- 未使用的旧 `wxPaymentSDK` 作为兼容归档保留在项目根目录，不进入小程序编译根。

## 边界核验

`miniprogram/` 顶层只包含：

```text
app.js
app.json
app.wxss
components/
config/
images/
pages/
utils/
```

核验结果：

- `miniprogram/admin-web`：不存在。
- `miniprogram/cloudfunctions`：不存在。
- `miniprogram/web-app`：不存在。
- 原 `zhuangzhuangji-main/web-app`：不存在。
- 独立网站 `zhuangzhuangji-web/package.json`：存在。
- 小程序编译根中未发现 Next.js、Turbopack 或服务端API构建代码。

## 验证结果

- 跨模块自动化契约测试：33/33通过。
- 异步生图结构验证：通过。
- 独立网站单元测试：5/5通过。
- 独立网站TypeScript检查：通过。
- 独立网站Next.js生产构建：通过。
- 运营后台Vite生产构建：通过。
- 迁移后微信开发者工具冒烟测试：15/15通过。
- 迁移后页面完整回归：12/12通过。
- 微信开发者工具控制台错误：0。
- 小程序运行时异常：0。
- 原 `web-app/.next` Source Map错误未再出现。

## 后续约束

详细规则见《项目目录隔离规范.md》。以后新增网站文件只能进入 `zhuangzhuangji-web`；新增小程序文件只能进入 `miniprogram/`；后台与云函数继续通过各自目录开发和部署。
